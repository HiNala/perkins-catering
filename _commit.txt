feat: larger text/buttons, polished blog, local SEO, AEO, lead funnels

Typography & UI:
- Increase base body font size from 16px to 17px with 1.7 line-height
- Bump button sizes: sm text-sm, md text-base, lg text-lg with more padding
- Bump all eyebrow labels from text-xs to text-sm across all pages
- Bump page header subtitles from text-lg to text-xl
- Increase Markdown blog content to text-lg with more spacing
- Increase CTA banner subtitle from text-lg to text-xl

Blog polish:
- Add read time estimates to blog list and detail pages
- Add featured post layout (large horizontal card) on blog index
- Add author avatar with initials and title on blog detail page
- Add inline CTA box at bottom of blog posts ("Planning an Event?")
- Add read time to related posts cards
- Add publisher info to BlogPosting JSON-LD
- Add breadcrumb JSON-LD to blog detail pages
- Better card design with dividers and metadata footer

Local SEO:
- Add geo coordinates (lat/long) to business config
- Add postal address to business config
- Add 14 service cities for local SEO coverage
- Add geo meta tags (geo.region, geo.position, ICBM) to root layout
- Add PostalAddress, GeoCoordinates to LocalBusiness JSON-LD
- Add City-level areaServed entries to JSON-LD
- Add openingHoursSpecification to JSON-LD
- Add service cities grid on homepage service area section
- Add service cities to contact page
- Expand keywords with city-specific terms (Santa Rosa, Petaluma, etc.)

AEO (Answer Engine Optimization):
- Add faqJsonLd() helper for reusable FAQ schema
- Add breadcrumbJsonLd() helper for navigation context
- Add FAQ schema to homepage (top 4 FAQs)
- Refactor resources page to use faqJsonLd helper

Lead funnels:
- Add inline CTA on blog detail pages with dual button (Inquiry + Menu)
- Add mt-6 spacing wrapper for contact page CTA button
- All public pages already have CTABanner at bottom

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
