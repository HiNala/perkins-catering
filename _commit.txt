fix: 12 bug fixes from deep security and code audit

Security:
- Add verifySession() to blog server actions (createPost, updatePost,
  deletePost) — they were public HTTP endpoints with no auth check
- Protect GET /api/analytics with session check (was publicly exposing
  analytics data)
- Disable public signup by default (ALLOW_SIGNUP env var required) —
  anyone could previously create an admin account
- Escape user-provided values in email HTML template to prevent HTML
  injection in notification emails
- Validate referrer URLs in analytics page before using as href to
  prevent javascript: protocol XSS

Reliability:
- Check sendEmail return value in inquiry/quote API routes and log
  error if email notification fails (was silently ignored)
- Revalidate old slug path in updatePost when slug changes (prevents
  stale cache at old URL)
- Add error handling to DeletePostButton (was silently failing on
  network errors with no user feedback)
- Install wget in Docker Alpine image for healthcheck (was missing,
  causing container to be marked unhealthy)

Bug fixes:
- Fix BlogForm date formatting using local timezone instead of UTC
  (was showing wrong date in non-UTC timezones)
- Replace hardcoded production URLs in blog/[slug] with SITE_URL
- Add useTransition loading state to AdminNav logout button

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
