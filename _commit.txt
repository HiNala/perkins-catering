fix: hydration, Docker standalone, DB indexes, hardcoded values

- Fix React #418 hydration error on admin inquiries page: use mounted
  state with requestAnimationFrame to defer date rendering to client
- Add output: "standalone" to next.config.ts for proper Docker builds
- Update Dockerfile to use standalone output (smaller image, no
  node_modules copy) and add HEALTHCHECK
- Add app healthcheck to docker-compose.yml
- Add DB indexes on inquiries.submittedAt, quotes.submittedAt,
  analyticsEvents.timestamp, analyticsEvents.type, blogPosts.status,
  blogPosts.publishedAt for query performance
- Fix CTABanner to use business.phoneHref instead of hardcoded phone
- Fix robots.ts to use NEXT_PUBLIC_SITE_URL env var instead of
  hardcoded URL
- Fix Header mobile menu: add onClick={closeMobileMenu} to phone link

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
