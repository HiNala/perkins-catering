feat: serve all images from S3 via imageUrl() helper

All visible images now route through /api/images which fetches from
the S3-compatible bucket (Railway Bucket in prod, MinIO in dev).
Falls back to public/ when S3 is not configured. Updated HeroSlider,
Footer, photos, bio, homepage, and inquire pages. OG/JSON-LD images
remain as static paths for social media crawlers.

Also fixed _upload_images.js to recurse into subdirectories and
uploaded all 16 images to the prod bucket.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
