feat: hero fixes, favicon, Stripe checkout, Resend emails, admin notifications

Hero page:
- Removed scroll indicator that overlapped buttons
- Moved location banner below the CTA buttons
- Added subtle drop shadows to heading, subtext, and buttons
- Centered all copy

Favicon:
- Generated SVG favicon from logo wheat/leaf emblem on sage background
- Generated PNG favicon (32x32), apple-touch-icon (180x180), PWA icons
- Updated manifest.ts with proper icon set
- Added icons metadata to root layout

Summer meals page (ecommerce):
- Complete rewrite with hero image background
- Split card layout: meal image + details with order form
- Stripe checkout API route (/api/checkout) with coming-soon fallback
- When STRIPE_SECRET_KEY is not set, shows "Buy Now — Coming Soon"
- Order form: name, email, phone, quantity selector, delivery toggle, notes
- Order summary with live total calculation
- Prominent 3-step pickup instructions section
- Pickup details card with location, hours, delivery info
- Order intents saved to DB even when Stripe is off

Resend email wiring:
- Added buildPurchaseEmail() for order notifications
- Added buildClientConfirmationEmail() for client auto-replies
- Inquiry and quote routes now send confirmation emails to clients
- Checkout route sends purchase notification to owner
- All emails gracefully skip when RESEND_API_KEY is not set

Admin dashboard notifications:
- New /api/notifications route returning recent submissions (24h window)
- NotificationsBell component with red blinking dot for critical leads/orders
- Polls every 60 seconds for new notifications
- Dropdown panel showing recent inquiries, quotes, and orders
- Type-specific icons (order=gold, quote=blue, inquiry=sage)
- Integrated into AdminNav header

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
